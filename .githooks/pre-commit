#!/bin/sh
# .githooks/pre-commit
# Synchronisiert die Versionsnummer automatisch vor jedem Commit.
# Einmalige Einrichtung: git config core.hooksPath .githooks

REPO_ROOT="$(git rev-parse --show-toplevel)"
php "$REPO_ROOT/version_sync.php"
